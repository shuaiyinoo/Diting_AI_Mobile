# ── 后端 API ──
VITE_API_BASE_URL=https://api.diting.example.com

# ── STOMP 信令 ──
# 生产必须是 wss://，且控制端固定用 /ws/mobile 端点
VITE_STOMP_URL=wss://api.diting.example.com/ws/mobile

# ── Coturn ──
# 生产建议同时提供 udp / tcp / tls 三种，提高穿透成功率
VITE_TURN_URL=turn:turn.diting.example.com:3478

VITE_ENABLE_MOCK=false
