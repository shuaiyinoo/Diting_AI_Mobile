# ── 后端 API（统一加 /apic 前缀）──
VITE_API_BASE_URL=https://cloud.ditingrag.com/apic

# ── STOMP 信令 ──
# 生产必须是 wss://，且控制端固定用 /apic/ws/mobile 端点
# WebSocket 端点同样挂在 servlet context-path 下，需要加 /apic 前缀
VITE_STOMP_URL=wss://cloud.ditingrag.com/apic/ws/mobile

# ── Coturn ──
# 生产建议同时提供 udp / tcp / tls 三种，提高穿透成功率
VITE_TURN_URL=turn:coturn.ditingrag.com:3478

VITE_ENABLE_MOCK=false
